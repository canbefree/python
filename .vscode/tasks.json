{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "crawl",
            "type": "shell",
            "command": "${workspaceFolder}/venv/Scripts/scrapy.exe",
            "args": [
                "crawl",
                "baidu"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/difa"
            }
        }
    ]
}